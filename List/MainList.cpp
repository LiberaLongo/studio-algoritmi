#include <iostream>
using namespace std;

#include "List.hpp"
#include "List.cpp"
/*
template<typename T>
void print(List<T> t) {
	cout << "inizio print";
	ListElement<T> * pos;
	while (!t.finisched()) {
		cout << t.read(pos);
		pos = t.next();
	}
	cout << "fine print";
}
*/
main() {
	//ListElement<int> * pos;
	List<int> t ();
	/*pos = t.head();
	pos = t.insert(pos, 1);
	pos = t.next();
	pos = t.insert(pos, 2);
	pos = t.next();
	pos = t.insert(pos, 3);
	print(t);*/
}
